---
schema_version: 1
algorithm_id: TLC-ALG-13-PRACTICE-006
feature_id: TLC-FC-13-PRACTICE-006
status: selected_for_practice_implementation_specification
ir_ref: registry/optimized-ir/practice/TLC-FC-13-PRACTICE-006/ir.yaml
signature: construct_blocked_practice_equation_candidate(request, opaque_inputs[2]) -> PracticeStructuralResult | PracticeError
inputs: [PracticeStructuralRequest, two keyed OpaqueCandidateScientificInput values]
outputs: [PracticeFeatureRepresentation, structured decision error]
validations: [exact identity, both required inputs, object order, exact scientific decision set]
preconditions: [inputs are supplied under candidate status]
prerequisites: []
conditions_of_application: [structural operations are allowed, scientific evaluation is deferred]
ordered_steps_declared: false
branches:
- {condition: structural operation, action: perform without equation evaluation}
- {condition: scientific execution, action: return external_executor_required}
- {condition: scientific evaluation, action: return scientific_decision_required with exact references}
repetitions: []
transitions: []
stop_conditions: []
effects: []
errors: [invalid_feature_identity, missing_required_input, invalid_input_type, provenance_mismatch, structural_error, scientific_decision_required, external_executor_required, implementation_out_of_scope]
postconditions: [objects, inputs, reservations and decisions are preserved]
invariants: [no threshold is selected, no collective algorithm is inferred]
determinism: {structural_operations: deterministic, scientific_result: blocked}
edge_cases: [missing one input fails, partial or extended decision set fails, unknown operation is out of scope]
dependencies: {execution: external_executor_required, scientific_review: required_before_evaluation}
unresolved_preserved: []
pseudocode: |
  REQUIRE feature id 006, inputs 006-1 and 006-2, objects [062, 091], and exact decision ids
  BUILD immutable declarative representation
  PRESERVE payloads and provenance without interpretation
  IF request is structural THEN RETURN requested structural result
  IF request is scientific_execute THEN RETURN external_executor_required
  IF request is scientific_evaluate THEN RETURN scientific_decision_required with exact references
  RETURN implementation_out_of_scope
