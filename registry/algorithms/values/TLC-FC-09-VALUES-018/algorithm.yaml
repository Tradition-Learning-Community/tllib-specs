feature_id: TLC-FC-09-VALUES-018
signature: source_ir_entrypoint(exact_source_ir_inputs) -> exact_source_ir_outputs
inputs: exact_source_ir_inputs
outputs: exact_source_ir_outputs
validations: source_ir_plus_identity_scope_context_and_endpoints
preconditions: source_ir_preconditions
conditions_of_application: explicit_relation_registration_only
steps: [validate_references_and_endpoints, execute_exact_source_ir_operation_sequence, attach_trace, return]
branching: named_error_or_external_evaluator_required
comparisons: source_declared_only
effects: no_endpoint_identity_edge_semantics_or_evaluation_invented
postconditions: source_ir_postconditions
invariants: source_ir_invariants
determinism: deterministic_for_structure
unresolved: propagate_all
pseudocode: |
  validate_source_ir_inputs_references_and_endpoints(inputs)
  result = execute_source_ir_operations_in_declared_order(inputs)
  return attach_complete_source_trace(result)
source_ir_preserved: true
source_contract_preserved: true
