schema_version: 1
feature_id: TLC-FC-11-CAPACITIES-011
algorithm_id: CAP-ALG-011
status: selected_for_capacities_implementation_specification
source_ir: registry/optimized-ir/capacities/TLC-FC-11-CAPACITIES-011/ir.yaml
signature: 'CapacityMetricDescriptor describe_riemannian_metric_symbolic_structure(FeatureId, List<OpaqueScientificObject>, Map<ObjectId,List<UnevaluatedSymbol>>, Map<ObjectId,SourceReference>, DecisionBlocker)'
inputs: {required_object_order: [TLC-SO-CAPACITIES-054]}
outputs: [CapacityMetricDescriptor]
validations: [exact feature id, exact singleton object, explicit symbol entry, complete provenance, exact blocker, structural request]
preconditions: [Riemannian source object present]
conditions_of_application: [no metric or distance evaluator]
steps:
  - validate targeted envelope
  - create symbolic metric descriptor for object 054
  - preserve capacity-space and g tokens
  - preserve geodesic and distance annotations as opaque
  - attach provenance, blocker and guard
  - return immutable descriptor
branching: {invalid_membership: MissingCoveredObject or UnexpectedCoveredObject, missing_symbol: MissingSymbolTableEntry, missing_blocker: MissingDecisionBlocker, evaluation_requested: ScientificEvaluationRequested}
capacity_handling: {construction: metric descriptor only, identity_validation: exact, scope_validation: singleton, context_validation: provenance}
relations: none
activation: not_performed
mobilization: not_performed
development: not_performed
comparison: no_distance_or_capacity_comparison
effects: no geometry calculation
errors: [UnknownFeatureId, MissingCoveredObject, UnexpectedCoveredObject, ObjectOrderMismatch, MissingSymbolTableEntry, MissingSourceReference, MissingDecisionBlocker, ScientificEvaluationRequested]
postconditions: [metric symbols preserved, annotations opaque, blocker unchanged]
invariants: [no distance, geodesic or norm computation]
determinism: deterministic
case_limits: [extra object fails, absent g token remains absent rather than invented, distance request fails]
unresolved_conserved: [scientific decision required]
dependencies: []
pseudocode: |
  require feature_id == TLC-FC-11-CAPACITIES-011 else UnknownFeatureId
  require exact_order(objects, [054]) else membership_or_order_error
  require symbol_entry(symbol_table, 054) else MissingSymbolTableEntry
  require complete_provenance(provenance) else MissingSourceReference
  require blocker == "scientific decision required" else MissingDecisionBlocker
  require request_is_structural_only() else ScientificEvaluationRequested
  descriptor = symbolic_metric_descriptor(objects[0], symbol_table[054])
  descriptor.attach(provenance, blocker, non_evaluation_guard)
  return immutable(descriptor)
