schema_version: 1
feature_id: TLC-FC-11-CAPACITIES-015
algorithm_id: CAP-ALG-015
status: selected_for_capacities_implementation_specification
source_ir: registry/optimized-ir/capacities/TLC-FC-11-CAPACITIES-015/ir.yaml
signature: 'CapacityValidityDomainPredicateAst build_validity_domain_predicate_ast(FeatureId, List<OpaqueScientificObject>, Map<ObjectId,List<UnevaluatedSymbol>>, Map<ObjectId,SourceReference>, DecisionBlocker)'
inputs: {required_object_order: [TLC-SO-CAPACITIES-005]}
outputs: [CapacityValidityDomainPredicateAst]
validations: [exact feature id, exact singleton object, explicit symbol entry, complete provenance, exact blocker, structural request]
preconditions: [validity-domain source object present]
conditions_of_application: [predicate representation only]
steps:
  - validate targeted envelope
  - create opaque predicate root
  - add only source-present tuple, product-domain, affinity, time, environment-membership and comparison tokens
  - attach provenance, blocker and non-evaluation guard
  - return immutable AST
branching: {invalid_membership: MissingCoveredObject or UnexpectedCoveredObject, missing_symbol: MissingSymbolTableEntry, missing_blocker: MissingDecisionBlocker, predicate_evaluation_requested: ScientificEvaluationRequested}
capacity_handling: {construction: predicate AST only, identity_validation: exact, scope_validation: singleton, context_validation: provenance}
relations: none
activation: not_performed
mobilization: not_performed
development: not_performed
comparison: token_representation_only_no_result
effects: no threshold or context decision
errors: [UnknownFeatureId, MissingCoveredObject, UnexpectedCoveredObject, ObjectOrderMismatch, MissingSymbolTableEntry, MissingSourceReference, MissingDecisionBlocker, ScientificEvaluationRequested]
postconditions: [only source-present predicate tokens included, blocker unchanged, no boolean result]
invariants: [no threshold generated, no environment or affinity score]
determinism: deterministic
case_limits: [absent token remains absent, extra object fails, predicate evaluation fails]
unresolved_conserved: [scientific decision required]
dependencies: []
pseudocode: |
  require feature_id == TLC-FC-11-CAPACITIES-015 else UnknownFeatureId
  require exact_order(objects, [005]) else membership_or_order_error
  require symbol_entry(symbol_table, 005) else MissingSymbolTableEntry
  require complete_provenance(provenance) else MissingSourceReference
  require blocker == "scientific decision required" else MissingDecisionBlocker
  require request_is_structural_only() else ScientificEvaluationRequested
  ast = opaque_predicate_ast(objects[0], source_present_tokens(symbol_table[005]))
  ast.attach(provenance, blocker, non_evaluation_guard)
  return immutable(ast)
