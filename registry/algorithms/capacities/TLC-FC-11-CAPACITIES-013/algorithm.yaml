schema_version: 1
feature_id: TLC-FC-11-CAPACITIES-013
algorithm_id: CAP-ALG-013
status: selected_for_capacities_implementation_specification
source_ir: registry/optimized-ir/capacities/TLC-FC-11-CAPACITIES-013/ir.yaml
signature: 'CapacityPrototypeDescriptor build_capacity_operator_signature_descriptor(FeatureId, Map<ObjectId,OpaqueScientificObject>, Map<ObjectId,SourceReference>)'
inputs: {required_object_order: [TLC-SO-CAPACITIES-049]}
outputs: [CapacityPrototypeDescriptor]
validations: [exact feature id, exact singleton object, no duplicate, complete provenance, structural request]
preconditions: [emergent-capacity source object present]
conditions_of_application: [signature representation only]
steps:
  - validate feature and object set
  - copy object and symbol as opaque values
  - construct immutable operator-signature descriptor
  - attach provenance and non-evaluation guard
  - return descriptor
branching: {missing_object: MissingCoveredObject, duplicate_object: DuplicateCoveredObject, missing_provenance: MissingSourceReference, application_requested: ScientificEvaluationRequested}
capacity_handling: {construction: signature descriptor only, identity_validation: exact, scope_validation: singleton, context_validation: provenance}
relations: none
activation: not_performed
mobilization: not_performed
development: not_performed
comparison: identifier_and_shape_only
effects: no operator application or capacity creation
errors: [UnknownFeatureId, MissingCoveredObject, DuplicateCoveredObject, MissingSourceReference, ScientificEvaluationRequested]
postconditions: [source symbol and provenance preserved, no domain or codomain inferred]
invariants: [opaque symbol unexecuted, no capacity generated]
determinism: deterministic
case_limits: [extra object fails, application request fails, missing signature details remain absent]
unresolved_conserved: []
dependencies: []
pseudocode: |
  require feature_id == TLC-FC-11-CAPACITIES-013 else UnknownFeatureId
  require exact_keys(payloads) == [TLC-SO-CAPACITIES-049] else MissingCoveredObject
  require no_duplicates(payloads) else DuplicateCoveredObject
  require complete_provenance(provenance) else MissingSourceReference
  require request_is_structural_only() else ScientificEvaluationRequested
  descriptor = immutable_operator_signature_descriptor(feature_id, opaque(payloads[049]), provenance[049])
  descriptor.attach_non_evaluation_guard()
  return descriptor
