# Invariants semantic analysis

The extraction identifies 37 objects typed as `Invariant`.
Temporal, numerical, structural, and relational interpretations are recorded only when source statements support them.
Computability remains `not_yet_determinable`; no executable claim is made.
