# TLC-FC-01-DISCIPLE-004 — Locally blocked dynamics

This package emits a deterministic blocked structural descriptor for trajectory dynamics while preserving the local scientific blocker and the symbol-only Master dependency. Descriptor construction remains available; scientific execution is refused with `DISCIPLE_UNRESOLVED_SEMANTICS`.

No trajectory transition, evolution result, or numerical behavior is inferred. Normative behavior is in `contract.json` and `acceptance.json`; evidence is in `traceability.json`.
