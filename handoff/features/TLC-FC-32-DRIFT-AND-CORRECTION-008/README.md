# TLC-FC-32-DRIFT-AND-CORRECTION-008

Conditional evaluator for the exact dogmatization expression with explicit zero guards on both denominators. `Delta_dog` remains unbounded unless external assumptions say otherwise; it is never clamped or converted to a probability.
